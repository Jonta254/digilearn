# No payment provider is active during open-preview access.
# Paystack variables will be documented only when server authentication,
# durable orders, entitlements, signed webhooks and reconciliation are ready.
